

Fisher Yates Shuffle
빠르고 정확한 셔플 방법
Math.floor(Math.random*I+1) 이 부분은 0이상 I 미만인 랜덤숫자

function FisherYatesShuffle(arr = []) {
    for (let i = arr.length - 1; i > 0; --i) {
        let j = Math.floor(Math.random() * (i + 1));
        [arr[i], arr[j]] = [arr[j], arr[i]];
    }
}
